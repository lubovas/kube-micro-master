package com.lubo.domain

object Constants {

  val WorkerHost = "worker-service"
  val WorkerPort = 8080
  val UsersPath = Seq("users")

  val ApplicationJson = Map("content-type" -> "application/json")
}
